RSA project

1. First Part
	- To compile project call make 
	- to run the key generation call './rsa435' this will generate the rsa keys

2. Second Part
	- To compile the project, when you are located in the project folder call make
	- To run sign a file called 'file.txt' call './messageDigest435 s file.txt'
	- This will sign the file and save it to a folder called file.txt.signature
	- To verify a signature of a file called 'file.txt' stored in a file called 'file.txt.signature' call './messageDigest435 v file.txt file.txt.signature'